{
  "name": "sofus-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "build:check": "NEXT_DIST_DIR=.next-check next build && node scripts/check-lines.js",
    "check:lines": "node scripts/check-lines.js"
  },
  "dependencies": {
    "next": "13.2.4",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "engines": {
    "node": ">=20.17.0"
  }
}
