/* What played at the stand.

   Drop the audio into fuji3d/public/music/ and add a row here. `src` is
   optional — a row with no src still shows in the list, it just can't be
   played, so the tracklist can go up before the files do.

   { title, artist, src }
*/
export const TRACKS = [];
